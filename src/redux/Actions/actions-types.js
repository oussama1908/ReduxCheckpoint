// Action Type Constants

// This action type is used to add a new post (or task) to the Redux store.
export const ADD_POST = 'ADD_POST';

// This action type is used to remove a post (or task) from the Redux store.
export const REMOVE_POST = 'REMOVE_POST';

// This action type is used to update the title of a post (or task) in the Redux store.
export const UPDATE_POST_TITLE = 'UPDATE_POST_TITLE';

// This action type is used to toggle the status (done/undone) of a post (or task) in the Redux store.
export const TOGGLE_TASK_STATUS = 'TOGGLE_TASK_STATUS';

// This action type is used to filter tasks based on their status (e.g., all, done, undone).
export const FILTER_TASKS = 'FILTER_TASKS';
